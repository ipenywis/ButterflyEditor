// Custom Draft-js Classes and Methods to be used with the Editor, for internal added funtions and extra handling
// (Make sure to use those methods instead of the default ones for safety)
/*import { ContentState, EditorState } from "draft-js";

import Decorators from "../components/draft/decorators";

export const createWithContent = (contentState: ContentState): EditorState => {
  //Use it With Default Decorators we first assigned to the editorState when initialized
  return EditorState.createWithContent(contentState, Decorators());
}

EMPTY

*/
